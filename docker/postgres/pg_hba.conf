# pg_hba.conf - configuración para acceso en entorno de desarrollo

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Permitir acceso local sin contraseña
local   all             all                                     trust

# Permitir acceso remoto sin contraseña (solo en desarrollo)
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                    trust
